# tsslint-plugin-react

A [`tsslint`](https://github.com/johnsoncodehk/tsslint) plugin that allows additional refactors and diagnostics with your VSCode editor (or any editor that supports TypeScript's LSP).
